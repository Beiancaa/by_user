<nav class="navbar fixed-top navbar-expand-sm navbar-inverse bg-dark">
  <div class="container">
    <div dir="auto">
      <ul class="nav navbar-nav pull-left">        
        <% if user_signed_in? %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <div class= "nav-link"> 
                <h7><%= t 'views.user.new.hi' %></h7> <%= current_user.email %>
              </div>              
            </li>
            <li class="nav-item">
              <%= link_to t('views.user.new.log_out'), destroy_user_session_path, :method => :delete, class: "nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to t('views.user.new.edit_profile'), edit_user_registration_path,  class: "nav-link" %>
            </li>
          </ul>
        <% else%>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to t('views.user.new.sign_up'), new_user_registration_path, class:  "nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to t('views.user.new.log_in'), new_user_session_path, class:  "nav-link"%>
            </li>
          </ul>
        <% end %>
      </ul>
    </div>
    
    <ul class="nav navbar-nav pull-right">
      <!-- Dropdown  
      -->
      <li class="nav-item dropdown">
        <div class="nav-link dropdown-toggle" data-toggle="dropdown" id="navbardrop">
          <%= t 'views.select_language'%>
        </div>
        <div class="dropdown-menu">
          <div class="dropdown-item"><%= link_to_unless I18n.locale == :en, "english", locale: :en %></div>
          <div class="dropdown-item"><%= link_to_unless I18n.locale == :ar, "العربية", locale: :ar %></div>
        </div>
      </li>
    </ul>
  </div>
</nav>
